# Live Events Rules

- Live-event effects must be explicit modifiers, not hidden feature flags.
- Event windows must support deterministic start and end processing.
- Operators may trigger events only through auditable admin flows.
