# Jail Rules

- Jailed players cannot perform restricted actions while `jailedUntil` is in the future.
- Expired jail timestamps stop blocking automatically based on current time.
- Bail, jailbreak, and manual release tools are out of scope for this phase.
