# Backend Modules

Each domain module owns its business rules, APIs, and tests. Use the per-module docs before making edits.

Every module directory is pre-layered with `domain`, `application`, `infrastructure`, `api`, and `tests` folders so feature code can be placed without inventing new structure during implementation.
