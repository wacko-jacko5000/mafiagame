{
  "name": "@mafia-game/types",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"console.log('lint not yet wired')\"",
    "test": "vitest run --pool=threads --poolOptions.threads.singleThread",
    "typecheck": "tsc -p tsconfig.json"
  }
}
